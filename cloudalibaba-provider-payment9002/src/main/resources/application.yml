server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: dumbledog129:80 #配置Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'

